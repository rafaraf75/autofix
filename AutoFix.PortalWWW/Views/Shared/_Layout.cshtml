<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - AutoFix</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="d-flex flex-column min-vh-100">

    <!-- HEADER -->
    <header class="sticky-top">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" asp-controller="Home" asp-action="Index">AutoFix</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                @* <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Index">Strona główna</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="OpisFirmy">O nas</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="HistoriaFirmy">Historia</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Rezerwacja" asp-action="Create">Rezerwacja</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Portal" asp-action="DodajKlientaPojazd">Dodaj klienta/pojazd</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Privacy">Polityka prywatności</a></li>
                    </ul>
                </div> *@
                @await Html.PartialAsync("Odnosniki",
                (IEnumerable<AutoFix.Data.Data.CMS.Strona>)ViewBag.ModelStrony)
            </div>
        </nav>
    </header>

    <!-- MAIN -->
    <main class="flex-fill py-4 bg-light">
        @RenderBody()
    </main>
    <hr/>

    @await Html.PartialAsync("Aktualnosci",
            (IEnumerable<AutoFix.Data.Data.CMS.Aktualnosc>)ViewBag.ModelAktualnosci)
    <!-- FOOTER -->
    <footer class="bg-dark text-white text-center py-3 fixed-bottom">
        &copy; 2025 AutoFix | ul. Rajdowa 123, Wrocław | tel. 123-456-789
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
